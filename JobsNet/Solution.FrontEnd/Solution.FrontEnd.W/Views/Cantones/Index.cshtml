@model IEnumerable<Solution.FrontEnd.W.Models.Cantones>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NombreCanton)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdProvinciaNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NombreCanton)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdProvinciaNavigation.NombreProvincia)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdCanton">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdCanton">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdCanton">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
